version: 2

models:
  - name: fct_trips
    columns:
      # - name: trip_id
      #   tests:
      #     - unique
      #     - not_null

      - name: payment_type
        data_tests:
          - accepted_values:
              arguments:
                values: [1, 2, 3, 4, 5]
                quote: false